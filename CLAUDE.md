# Claude Memory File

## Commands & Scripts
- **Typechecking**: Use `turbo typecheck -F {workspace-name}` for workspace-specific typechecking

## Notes
- Prefer not to add comments
- Avoid type assertions (`as FooType`) as much as possible
